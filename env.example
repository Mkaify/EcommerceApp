# Database
DATABASE_URL="postgresql://username:password@localhost:5432/database_name"

# NextAuth
NEXTAUTH_SECRET="your-secret-key-here-replace-with-strong-random-string"
NEXTAUTH_URL="http://localhost:3000"

# For production deployment on Vercel:
# 1. Add DATABASE_URL in Vercel project settings (will be auto-generated if using Vercel Postgres)
# 2. Add NEXTAUTH_SECRET with a strong random string  
# 3. NEXTAUTH_URL will be auto-set by Vercel to your deployed domain 